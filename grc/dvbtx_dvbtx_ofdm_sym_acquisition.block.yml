# auto-generated by grc.converter

id: dvbtx_ofdm_sym_acquisition
label: OFDM Symbol Acquisition
category: '[Core]/Digital Television/DVB-TX'

parameters:
-   id: type
    label: Output Type
    dtype: enum
    default: complex
    options: [complex, float, int, short, byte]
    option_attributes:
        fcn: [c, f, i, s, b]
    hide: part
-   id: fft_length
    label: FFT Length
    dtype: int
    default: '8192'
-   id: occupied_tones
    label: Occupied Tones
    dtype: int
    default: '6817'
-   id: cp_length
    label: Cyclic Prefix Length
    dtype: int
    default: '256'
-   id: snr
    label: SNR
    dtype: real
    default: '30'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ fft_length }

templates:
    imports: import dvbtx
    make: dvbtx.dvbtx_ofdm_sym_acquisition(1, ${fft_length}, ${occupied_tones}, ${cp_length},
        ${snr})

file_format: 1
